//
//  OnboardingView.swift
//  GPTDetectorSwiftUI
//
//  Created by cem on 27.08.2023.
//

import SwiftUI

struct OnboardingView: View {
    @StateObject private var onboardingViewModel = OnboardingViewModel(cacheClient: CacheClient())
    @Binding var isOnboardingCompleted: Bool

    var body: some View {
        ZStack {
            Color.theme.backgroundColor
                .ignoresSafeArea()
            VStack(alignment: .leading, spacing: .spacing.veryHigh) {
                Header()
                Group {
                    ImageWithDescription(image: "hand.wave", description: "Welcome to GPT Detector! GPT Detector whether a given text has been generated by a large language model or not.")
                    ImageWithDescription(image: "character.bubble", description: "For the best results, we highly recommend using English text.")
                }
                .padding(.horizontal)
                Spacer()
                GetStartedButton(action: {
                    onboardingViewModel.completeOnboarding()
                }, isOnboardingCompleted: $isOnboardingCompleted)
            }
            .padding()
        }
    }
}

private struct Header: View {
    var body: some View {
        HStack {
            Spacer()
            ZStack(alignment: .bottom) {
                Image("LaunchScreen")
                    .resizable()
                    .scaledToFit()
                    .frame(width: .imageSize.xxLarge, height: .imageSize.xxLarge)
                Text("GPT Detector")
                    .font(.title)
            }
            Spacer()
        }
    }
}

private struct ImageWithDescription: View {
    let image: String
    let description: String

    var body: some View {
        HStack(spacing: .spacing.veryHigh) {
            Image(systemName: "character.bubble")
                .resizable()
                .scaledToFit()
                .frame(width: .imageSize.regular, height: .imageSize.regular)
                .foregroundColor(Color.theme.accentColor)

            Text("For the best results, we highly recommend using English text.")
            Spacer()
        }
    }
}

private struct GetStartedButton: View {
    let action: () -> Void
    @Binding var isOnboardingCompleted: Bool
    var body: some View {
        Button {
            action()
            isOnboardingCompleted = true
        } label: {
            Text("Get Started")
                .frame(maxWidth: .infinity, minHeight: .buttonSize.regular)
                .background(Color.theme.accentColor)
                .foregroundColor(Color.theme.secondaryTextColor)
                .font(.headline)
                .cornerRadius(.borderRadius.regular)
        }
    }
}

struct OnboardingView_Previews: PreviewProvider {
    static var previews: some View {
        NavigationStack {
            OnboardingView(isOnboardingCompleted: .constant(false))
        }
    }
}
